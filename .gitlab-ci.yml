stages:
  - build
  - test
  - deploy

create_file:
  image: alpine
  stage: build
  script:
    - echo "Creation des fichiers"
  artifacts:
    paths:
      - project
testing_file:
  image: alpine
  stage: test
  script:
    - echo "Tester les fichiers"
  artifacts:
    paths:
      - project
  
deploy_file:
  image: alpine
  stage: deploy
  script:
    - echo "Déploiement des fichiers"
  artifacts:
    paths:
      - project